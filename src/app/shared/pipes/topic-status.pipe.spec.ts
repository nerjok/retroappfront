import { TopicStatusPipe } from './topic-status.pipe';

describe('TopicStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
