import { DueDateColorDirective } from './due-date-color.directive';

describe('DueDateColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DueDateColorDirective();
    expect(directive).toBeTruthy();
  });
});
